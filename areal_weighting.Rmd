---
title: "Areal Weighting Tracts"
author: "Malia Cortez"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
library(dplyr)
library(tidyverse)
library(areal)
library(tidycensus)
```

## 

```{r cars}
wa.2020 <- tracts(state = "WA", year = 2020)
wa.2010 <- tracts(state = "WA", year = 2010)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
